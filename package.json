{
  "author": "Dario Sechi",
  "devDependencies": {
    "@microsoft/api-extractor": "^7.29.3",
    "@queelag/core": "^1.1.4",
    "@queelag/preact": "^1.0.0",
    "@queelag/web": "^1.0.1",
    "@queelag/web-components": "^1.0.0",
    "@rollup/plugin-strip": "^2.1.0",
    "@rollup/plugin-typescript": "^8.3.4",
    "@types/jest": "^28.1.7",
    "jest": "^28.1.3",
    "lit": "^2.3.1",
    "preact": "^10.11.0",
    "prettier": "^2.7.1",
    "rollup": "^2.78.1",
    "rollup-plugin-terser": "^7.0.2",
    "ts-jest": "^28.0.8",
    "tslib": "^2.4.0",
    "typedoc": "^0.23.10",
    "typescript": "4.7.4"
  },
  "license": "MIT",
  "main": "index.js",
  "name": "@queelag/preact-components",
  "peerDependencies": {
    "@queelag/core": ">= 1.1.0",
    "@queelag/preact": ">= 1.0.0",
    "@queelag/web": ">= 1.0.0",
    "@queelag/web-components": ">= 1.0.0",
    "preact": ">= 10.11.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": "https://github.com/queelag/preact-components",
  "scripts": {
    "test": "yarn jest --coverage",
    "build": "rm -rf dist && yarn rollup -c && yarn api-extractor run --local",
    "tarball": "yarn version && cp package.json dist/. && cd dist && yarn pack",
    "deploy": "yarn test && yarn build && yarn tarball && yarn publish dist/*.tgz --access public"
  },
  "sideEffects": false,
  "type": "module",
  "version": "1.0.2"
}

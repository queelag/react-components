import { createBaseElementComponent, ElementComponent, IconProps } from '@queelag/react'
import { IconElement } from '@queelag/web-components/elements/data/icon.element.js'

export const QIcon: ElementComponent<IconElement, IconProps> = createBaseElementComponent('q-icon', IconElement)

import { createElementComponent, ElementComponent, IconProps } from '@queelag/react'
import { IconElement } from '@queelag/web-components/elements/data/icon.element'

export const QIcon: ElementComponent<IconElement, IconProps> = createElementComponent('q-icon', IconElement)
